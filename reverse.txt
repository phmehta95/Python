#def reverse(text):
 # for i in range(len(text)):
  #  print text.[(len(text)-1)]
def reverse(text):
    stringg=''
    x=len(text)
    while(x>0):
        stringg=stringg+text[x+1]
        x=x-1
    return stringg
print reverse("Pruthvi!")